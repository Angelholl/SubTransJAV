# v1.2.1 验收报告

日期：2026-09-17 ｜ 决策依据：docs/decision-log.md D2026-0916-03（decision-critic 评议后拍板）｜ 状态：**代码全部落地、测试全绿，待实弹复跑验收后提交**

## 一、您反馈的 7 项问题逐项对照

| # | 您的反馈 | 核实根因 | 修复 | 验证 |
|---|---|---|---|---|
| ① | 报告假名残留大量重复（1-5 同为 #660） | 报告按"假名串"逐个展开，同一条目 N 串占 N 个编号（quality_report.py） | **按条目归并**：一条目一条，片段清单上限 5 段+"等 N 段"；结论行按条目数计 | `test_quality_report_kana_grouped_per_entry` |
| ② | 29 条实义台词被终稿丢弃（"应按字幕翻译，终稿处理有问题"） | 实测 **126 条被静默删除**（1162→1036）：阶段A"留空=删除"授权、规则清洗 L6-L12 短行删除、阶段B缺译文"整条移除"、语言过滤删非中文——闸门0 一条未删 | **删除权收归闸门0**：下游一律不删——A 每行必须产出（无法辨识→`[未翻译]`保留原文）、cleaner 删除前先查源文（源行含汉字一律保留）、B 缺译文回退 A 译文、语言过滤改为加 `[未翻译]` 标记保留 | e2e：模型拒不输出 → 终稿条目集合不变（`test_d1_mute_model_preserves_every_entry`）；`部長さん。`/`…最高…` 源侧证据 0 删除回归 |
| ③ | 原字幕时间轴合理、终稿时间轴有问题 | 保留条目时间戳与源**逐毫秒一致**；"问题"= ②删除造成的空洞 + LLM 行级合并致 18 条 >5 秒长字幕 | ②关闭后空洞消除；A/B 行数守卫（批后断言 + 缺行重试预算 2 次 + 降级 fallback，永不整文件失败）遏制行级合并拉长 | `test_missing_retry_budget_exactly_two_rounds`、`MergeTwoLinesClient` 端到端用例 |
| ④ | #2 主语误判（应译"…是我们的部长"） | **误报**：终稿实为"我们，"开头（正确），但校验正则把"我们是"也命中"我是" | 正则收窄到单数"我是/俺是"；告警动态引用实际译文开头；两张角色卡反例改写（定语读法"是我们的…"优先）并补您的实例 | `test_subject_plural_predicate_translation_no_warning`（"我们是/我们，是"不再告警） |
| ⑤ | #379 译文与原字幕意思完全相反（转录乱码被脑补成反向语义） | 两张角色卡均主动要求"补译/意译/增强张力"，全库无"乱码禁脑补"条款 | 四处同步新增硬条款：乱码只直译可辨部分、禁止臆造、**严禁反转语义方向**（拒绝↔邀请、停止↔继续）、无法辨识→`[未翻译]`严禁编造；报告新增【乱码强译复核】小节（乱码信号×流畅中文→人工复核清单）；语义黄金集 semantic_v1.json 回归 | `test_golden_set.py` 新增 3 测试（反转案例必须被标记） |
| ⑥ | 处置报告.json 多余、用户看不懂 | 仅覆盖闸门0（本次 deleted=0，与实际丢 126 条严重不符）、运行时无消费者 | **退场**：不再落盘；信息并入质量报告【处置】章节（删除台账+恒等式+乱码复核）；机器可读通道保留 gate0_summary NDJSON 事件；旧 json 残留会被 resume 清理自动扫除 | `test_gate0_summary_payload_and_disposal_section_contract`（json 不落盘负向断言） |
| ⑦ | A 环节只翻译不碰时间轴、B 润色审核（分工提案） | 可行：LLM 本就不接触时间码；越权的是 A 的删行与行级合并授权 | A=纯翻译（禁删禁并、1:1 映射）；B=审校+抛光+补译（含语义方向核查项）；时间轴/合并仍由代码层负责并如实入账 | 行数守卫测试 + 手册 §10 行为变化说明 |

另修复两项您未点名但同源的问题：
- 统计行"规则清洗合并 114 处"实为删除为主 → 拆分为"合并 X · 删除 Y（源侧证据免删 Z）"如实呈现；
- 新增**条数恒等式**核对行：`原文 = 闸门0删除 + 预合并合并 + 规则清洗合并 + 规则清洗删除 + 隔离区移出 + 终稿`，不平即 ⚠️——今后任何"静默丢条"在这行数学上无处遁形。

## 二、改动规模与质量门

- 27 个文件，+1671 / -321 行（含 1 个新黄金集 fixture）；版本号 1.1.0 → **1.2.1**（pyproject + `__version__.py`）；CHANGELOG `[1.2.1] - 2026-09-17`；手册新增 §10「1.2.1 行为变化一览」并重写 §7.2。
- **测试：765 passed / 1 skipped**（较基线净增约 30 个用例）；**ruff 全项目 All checks passed**。
- `tests/test_process_manager.py` 有 7 个失败为 **Windows 环境性预存问题**（进程树终止类，与本次改动零交集，改动前即失败），未动。
- 提示词已纳入 config 指纹：改提示词后 `--resume` 自动拒绝复用旧阶段产物（新增测试钉死）。

## 三、实弹复跑验收指引（需要您的 LLM API 额度，故留给您执行）

用您平时的 GUI/CLI 方式对同一条视频（4k2.me@mida-559，源 1162 条）**全新重跑**（不要 --resume 旧产物），按下面清单验收：

1. **终稿条数 ≈ 1160**（1162 − 预合并合并 2），不再是 1036；
2. 质量报告【统计】的**条数核对行显示 ✅**；
3. 抽查原被删时间点：00:18:50、00:25:34、00:35:14、00:38:21、00:43:53、00:59:46 处**均有译文**；
4. 原 #660（01:05:33）等假名残留条目：要么已译出，要么以 `[未翻译]` 前缀保留并列入报告【未翻译】小节（不再是无标记日文）；
5. 报告**假名残留章同一条目只出现一次**；#2 **不再出现在主语误判**；
6. 输出目录**不再生成 幻觉处置报告.json**；
7. 若模型把乱码行翻译得通顺，对应条目应出现在【乱码强译复核】小节——建议对照视频抽几条（这是 ⑤ 类问题的兜底网）。

## 四、遗留与风险（如实告知）

- ① 强制翻译纯噪声行（呻吟等）后，此类行会以直译形态（"啊…/唔…"）留在终稿，条目数较旧行为偏多——这是"宁多勿缺"的既定取舍，观感如不适可反馈调节；
- ② 主语误判正则收窄后，"我们是…"形态翻译对将进入 TM 学习面（有角色卡规则 + learn_gate 双层防护），建议复跑后抽检 TM 新增条目；
- ③ test_process_manager 的 7 个环境性失败建议另行排期（与本次无关）；
- ④ 全部改动**未提交**——待您验收通过后再 commit（决策日志状态已标"待实弹验收/提交"）。

— 验收报告完 —
